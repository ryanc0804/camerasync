# Postgres credentials shared by the `db` and `server` services in
# docker-compose.yml. Copy this file to `.env` (gitignored) and change the
# password before using this anywhere beyond local dev.
POSTGRES_USER=camerasync
POSTGRES_PASSWORD=camerasync
POSTGRES_DB=knighthyve
